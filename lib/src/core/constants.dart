import 'package:flutter/material.dart';

class AppConstants {
  static const radiusValue = 16.0;
  static const Radius radius = Radius.circular(radiusValue);
  static BorderRadius borderRadius = BorderRadius.circular(radiusValue);
}
